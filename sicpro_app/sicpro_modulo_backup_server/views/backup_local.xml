<?xml version="1.0" encoding="utf-8"?>
<!--
##############################################################################
PROYECTO: SICPRO ERP
AUTOR: Daniel Barrero Reyes (Danny Rose's)
CONTACTO: daniel.borrero@etecsa.cu
Copyright (C) 2020-2026 SICPRO ERP.
Todos los derechos reservados.
##############################################################################
-->
<odoo>
  <record id="sicpro_backup_local_form_view" model="ir.ui.view">
    <field name="name">sicpro.backup.local.form</field>
    <field name="model">sicpro.backup.local</field>
    <field name="type">form</field>
    <field name="arch" type="xml">
      <form>
        <header>
          <button name="confirm_process" class="oe_highlight" string="Confirmar" type="object"
                  invisible="(state != 'draft')"/>
          <button name="cancel_process" class="oe_highlight" string="Cancelar" type="object"
                  invisible="(state == 'running') or (state == 'cancel')"/>
          <button name="remove_attached_cron" class="oe_highlight" string="Cancelar proceso" type="object"
                  invisible="(state != 'running')"/>
          <field name="state" widget="statusbar"/>
        </header>
        <sheet>
          <div class="" style="width: 500px;">
            <div class="oe_title" style="width: 500px;">
              <label class="oe_edit_only" for="name" string="Name"/>
              <h1><field name="name" class="oe_inline" readonly="1"/></h1>
            </div>
          </div>
          <div class="row">
            <div class="col-md-6">
              <group>
                <field name="frequency_cycle" required="1"/>
                <field name="backup_starting_time" required="1" readonly="state != 'draft'"/>
                <field name="db_name" required="1" readonly="state != 'draft'"/>
                <field name="master_pass" required="1" readonly="state != 'draft'"/>
                <field name="backup_format" required="1" readonly="(state != 'draft')"/>
              </group>
            </div>
            <div class="col-md-6">
              <group>
                <field name="backup_location" required="1" readonly="state != 'draft'"/>
                <label for="remote_server_id" invisible="backup_location != 'remote'"
                       required="backup_location == 'remote'" readonly="state != 'draft'"/>
                <div class="mb-2" invisible="backup_location != 'remote'">
                  <field name="remote_server_id" invisible="backup_location != 'remote'"
                         required="backup_location == 'remote'" readonly="state != 'draft'"/>
                  <br/>
                  <button string="Conexión de prueba" name="test_host_connection" type="object"
                          class="oe_highlight" invisible="not remote_server_id"/>
                </div>
                <field name="storage_path" required="1" readonly="state != 'draft'"/>
                <field name="enable_retention" readonly="state != 'draft'"/>
                <field name="retention" required="1" readonly="state != 'draft'"
                       invisible="not enable_retention"/>
              </group>
              <div class="text-muted mb-3" name="retention_note">
                <strong invisible="not enable_retention and  backup_location in ['local', 'remote']">
                  Nota:</strong>
                <ul>
                  <li invisible="not enable_retention">Después de habilitar la copia de seguridad
                    de Drop Database, debe configurar el recuento de retención de copias de
                    seguridad.</li>
                  <li invisible="not enable_retention">El recuento de retención de copias de
                    seguridad es la cantidad de copias de seguridad que desea conservar..</li>
                </ul>
              </div>
            </div>
          </div>
          <notebook>
            <page name="backup_details" string="Detalles de la copia de seguridad">
              <field name="backup_details_ids" readonly="1">
                <list editable="bottom" create='false'>
                  <field name="backup_date_time" readonly="1"/>
                  <field name="file_name" readonly="1"/>
                  <field name="message" readonly="1"/>
                  <field name="status" readonly="1" widget="badge"
                         decoration-success="status == 'Success'"
                         decoration-danger="status =='Failure'"/>
                  <field name="backup_location" invisible="1"/>
                  <button name="unlink_confirmation" type="object" title="Borrar" icon="fa-trash"
                          class="btn-secondary me-2 text-muted"/>
                  <button name="download_db_file" type="object" string="Descargar"
                          class="oe_highlight"
                          invisible="status != 'Success' or backup_location not in ['local', 'remote']"/>
                </list>
              </field>
            </page>
          </notebook>
        </sheet>
        <chatter/>
      </form>
    </field>
  </record>

  <record id="sicpro_backup_local_list_view" model="ir.ui.view">
    <field name="name">sicpro.backup.local.list</field>
    <field name="model">sicpro.backup.local</field>
    <field name="type">list</field>
    <field name="arch" type="xml">
      <list>
        <field name="name"/>
        <field name="frequency_cycle"/>
        <field name="db_name"/>
        <field name="storage_path"/>
        <field name="backup_location"/>
        <field name="state" widget="badge" decoration-info="state == 'draft'"
               decoration-warning="state =='confirm'" decoration-success="state =='running'"
               decoration-danger="state =='cancel'"/>
      </list>
    </field>
  </record>

  <record id="sicpro_backup_local_action_view" model="ir.actions.act_window">
    <field name="name">Copias de seguridad</field>
    <field name="path">backup-process</field>
    <field name="res_model">sicpro.backup.local</field>
    <field name="view_mode">list,form</field>
    <field name="view_id" ref="sicpro_backup_local_list_view"/>
    <field name="help" type="html">
      <p class="o_view_nocontent_smiling_face">
        Crear nuevo proceso de copia de seguridad
      </p>
      <p>
        Configure los procesos de copia de seguridad para crear las copias de seguridad de la base de datos.
      </p>
    </field>
  </record>
</odoo>
