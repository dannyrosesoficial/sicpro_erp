<?xml version="1.0"?>
<odoo>

        <record id="crm_lost_reason_view_search" model="ir.ui.view">
            <field name="name">sicpro.app.solicitudes.rechazadas.search</field>
            <field name="model">sicpro.app.solicitudes.rechazadas</field>
            <field name="arch" type="xml">
                <search string="Buscar Oportunidad">
                    <filter string="Incluir archivados" name="archived" domain="['|', ('active', '=', True),
                    ('active', '=', False)]"/>
                    <separator/>
                    <filter string="Archivado" name="inactive" domain="[('active', '=', False)]"/>
                </search>
            </field>
        </record>

        <record id="crm_lost_reason_view_form" model="ir.ui.view">
            <field name="name">sicpro.app.solicitudes.rechazadas.form</field>
            <field name="model">sicpro.app.solicitudes.rechazadas</field>
            <field name="arch" type="xml">
                <form string="Canal">
                    <sheet>
                        <widget name="web_ribbon" title="Archivado" bg_color="bg-danger"
                                attrs="{'invisible': [('active', '=', True)]}"/>
                        <div class="oe_title">
                            <div class="oe_edit_only">
                                <label for="name"/>
                            </div>
                            <h1>
                                <field name="name"/>
                            </h1>
                            <field name="active" invisible="1"/>
                        </div>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="crm_lost_reason_view_tree" model="ir.ui.view">
            <field name="name">sicpro.app.solicitudes.rechazadas.tree</field>
            <field name="model">sicpro.app.solicitudes.rechazadas</field>
            <field name="arch" type="xml">
                <tree string="Canal" editable="bottom">
                    <field name="name"/>
                </tree>
            </field>
        </record>

        <record id="crm_lost_reason_action" model="ir.actions.act_window">
            <field name="name">Motivo de rechazo</field>
            <field name="res_model">sicpro.app.solicitudes.rechazadas</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Defina los motivos de rechazo
              </p><p>
                Aquí puede describir los motivos de rechazo de una solicitud de trabajo.
              </p>
            </field>
        </record>

        <menuitem
            id="menu_solicitudes_rechazo" action="crm_lost_reason_action"
            parent="solicitudes_menu_config"
            groups="grupo_app_solicitud_responsable" sequence="6"/>


</odoo>