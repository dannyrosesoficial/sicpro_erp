<?xml version="1.0"?>
<odoo>

    <record id="note_tableros_rule_global" model="ir.rule">
        <field name="name">Regla de tableros de Notas</field>
        <field name="model_id" ref="model_note_tableros"/>
        <field name="domain_force">['|',('user_id','=',False),('user_id','=',user.id)]</field>
    </record>

</odoo>
